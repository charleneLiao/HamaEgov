define(['cookie', 'jquery', 'group'], function(cookie, $, _){

	function main(env, opt, file){

		// var $env = $(env).find('.content'),
		// 	$root = $('.sys-root');

		console.log(_.getInner(env));
		
		// var _cookie_name = 'font-size',
		// 	_default_size = 'medium';

		// if ( cookie.get('font_size')) { cookie.set(_cookie_name, _cookie_name +'-'+ _default_size) } //找cookie，沒有就給預設值
		
		// $env.on('click', 'a', function(){
		// 	var $this = $(this);
		// });
	}
	
	return main;
});
