<%
	min_text = 4 #最少的字元
	max_text = 8 #最多的字元
%>

<div class="simple-text content-text <% if class_set %><%= class_set %><% end %>" data-index="<%= index %>" data-type="<%= $module %>" <% if func_set %> data-func="<%= func_set %>"<% end %> ><div class="in">
	<% if header_text %>
		<div class="hd"><div class="in">
			<h4><span><a href="#"><%= header_text %></a></span></h4>
		</div></div>
	<% end %>
	<div class="ct"><div class="in">
		<span><a href="#"><%= zh_lorem_words (min_text..max_text).to_a.sample %></a></span>
	</div></div>
</div></div>